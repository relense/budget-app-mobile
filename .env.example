# Backend base URL, read by app.config.ts at build/start time (see src/lib/apiUrl.ts
# for the Android-emulator localhost rewrite). Copy this file to .env and adjust if your
# local backend isn't on the default port.
API_URL=http://localhost:4400
